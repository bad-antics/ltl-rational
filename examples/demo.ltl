// examples/demo.ltl — runnable demo for ltl_rational

import std::io
use ltl_rational as lib

fn main() {
    io::println("ltl_rational demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}